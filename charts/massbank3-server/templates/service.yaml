apiVersion: v1
kind: Service
metadata:
  name: {{ include "massbank3-server.fullname" . }}
  labels:
    {{- include "massbank3-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "massbank3-server.selectorLabels" . | nindent 4 }}
